country_decisions = {

    iram_02_cancel_all = {
        potential = {
            is_ai = no
        }
        highlight = { scope:province = { always = yes } }
        allow = {
            is_ai = no
            always = yes
        }
        effect = {
            exodos_cleanup_effect = yes
        }
        ai_will_do = { factor = 0 }
    }
}
