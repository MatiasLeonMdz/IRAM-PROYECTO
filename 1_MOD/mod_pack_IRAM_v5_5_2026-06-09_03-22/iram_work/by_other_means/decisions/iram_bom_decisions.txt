country_decisions = {

    iram_bom_confirm = {
        potential = {
            is_ai = no
            has_variable = iram_bom_menu_bom
            NOT = { has_variable = iram_bom_menu_ego }
            NOT = { has_variable = iram_bom_menu_heir }
        }
        highlight = { scope:province = { always = yes } }
        allow = {
            is_ai = no
            custom_tooltip = iram_tt_bom_rival
            any_character = { is_ruler = yes  num_of_rivals >= 1 }
        }
        effect = {
            every_character = {
                limit = { is_ruler = yes }
                every_rival = {
                    add_health = -100
                }
            }
        }
        ai_will_do = { factor = 0 }
    }

    iram_bom_bacanal = {
        potential = {
            is_ai = no
            has_variable = iram_bom_menu_bom
            NOT = { has_variable = iram_bom_menu_ego }
            NOT = { has_variable = iram_bom_menu_heir }
        }
        highlight = { scope:province = { always = yes } }
        allow = {
            is_ai = no
            custom_tooltip = iram_tt_bom_rival
            any_character = { is_ruler = yes  num_of_rivals >= 1 }
        }
        effect = {
            every_character = {
                limit = { is_ruler = yes }
                every_rival = {
                    remove_trait = chaste
                    add_trait = lustful
                }
            }
        }
        ai_will_do = { factor = 0 }
    }

    iram_bom_kill_ruler = {
        potential = {
            is_ai = no
            has_variable = iram_bom_menu_bom
            NOT = { has_variable = iram_bom_menu_ego }
            NOT = { has_variable = iram_bom_menu_heir }
        }
        highlight = { scope:province = { always = yes } }
        allow = {
            is_ai = no
        }
        effect = {
            every_character = {
                limit = { is_ruler = yes }
                add_health = -100
            }
        }
        ai_will_do = { factor = 0 }
    }

    iram_bom_seize_holdings = {
        potential = {
            is_ai = no
            has_variable = iram_bom_menu_bom
            NOT = { has_variable = iram_bom_menu_ego }
            NOT = { has_variable = iram_bom_menu_heir }
        }
        highlight = { scope:province = { always = yes } }
        allow = {
            is_ai = no
            custom_tooltip = iram_tt_bom_rival_unico
            any_character = {
                is_ruler = yes
                num_of_rivals >= 1
                NOT = { num_of_rivals >= 2 }
                any_rival = { employer = ROOT }
            }
        }
        effect = {
            every_character = {
                limit = { is_ruler = yes }
                every_rival = {
                    limit = { employer = ROOT }
                    while = {
                        limit = { num_holdings_owned > 0 }
                        random_holdings = {
                            remove_holding = prev
                        }
                    }
                    add_loyalty = family_property_seized_l
                }
            }
        }
        ai_will_do = { factor = 0 }
    }

    iram_bom_fill_the_void = {
        potential = {
            is_ai = no
            has_variable = iram_bom_menu_bom
            NOT = { has_variable = iram_bom_menu_ego }
            NOT = { has_variable = iram_bom_menu_heir }
        }
        highlight = { scope:province = { always = yes } }
        allow = {
            is_ai = no
            custom_tooltip = iram_tt_bom_rival_unico
            any_character = {
                is_ruler = yes
                num_of_rivals >= 1
                NOT = { num_of_rivals >= 2 }
                any_rival = { employer = ROOT }
            }
        }
        effect = {
            every_character = {
                limit = { is_ruler = yes }
                every_rival = {
                    limit = { employer = ROOT }
                    save_scope_as = iha_rival_scope
                }
            }
            every_owned_province = {
                limit = { NOT = { exists = holding_owner } }
                scope:iha_rival_scope = { add_holding = PREV }
            }
        }
        ai_will_do = { factor = 0 }
    }
}
